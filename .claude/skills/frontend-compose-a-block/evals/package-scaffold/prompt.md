---
name: package-scaffold
runs: 3
tags: [package, scaffold]
---

# Scaffold a package

Working in the `baseapp-frontend` submodule. Add a new workspace package
`packages/scheduling/`, published as `@baseapp-frontend/scheduling`.

Create the files the package always needs and tell me the command that builds
it. I do not need the helper implementations yet — the scaffold and the build
wiring is what I am after.
