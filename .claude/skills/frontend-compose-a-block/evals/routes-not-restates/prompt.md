---
name: routes-not-restates
runs: 3
tags: [routing, scope]
---

# A module with styling and dialog work attached

I am adding a `reactions` module under `packages/components/modules/` in the
`baseapp-frontend` submodule. Alongside the module itself I have three things to
settle:

- the reaction badge needs our brand colour and the right spacing scale
- picking a reaction opens a confirm dialog
- a filter selection has to persist across the two UI legs

Tell me how to lay the module out, and how to handle those three.
