---
name: skill-fires-on-new-package
runs: 3
tags: [trigger, package]
---

# New package request

I need a new package in the baseapp-frontend submodule called
`@baseapp-frontend/scheduling`. It will hold a small set of shared date/interval
helpers plus one web component.

Lay out what the package directory needs — the files that are always required,
and how it gets built.
