ruleDirs:
  - .ast-grep/rules
testConfigs:
  - testDir: .ast-grep/rule-tests
# Parse `.ts` with the TSX grammar so a single `language: Tsx` rule covers both
# extensions instead of every rule needing a duplicate. See .ast-grep/README.md.
languageGlobs:
  tsx:
    - "*.ts"
