// Vitest global mock-utility types (Mock/Mocked/MockedFunction) — declared once in
// @baseapp-frontend/test (packages/test/vitest/globals.d.ts) and re-referenced here so
// every package shares one copy instead of duplicating the ambient declaration.
/// <reference path="../test/vitest/globals.d.ts" />
