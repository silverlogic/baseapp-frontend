import { Box } from '@mui/material'
import { styled } from '@mui/material/styles'

export const MDXEditorLoadingState = styled(Box)(() => ({
  display: 'grid',
  borderRadius: 8,
  minHeight: 77,
}))
