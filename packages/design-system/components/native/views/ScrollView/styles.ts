import { StyleSheet } from 'react-native'

export const createStyles = () =>
  StyleSheet.create({
    scrollViewcontainer: {
      padding: 0,
      flex: 1,
    },
    keyboardHideContainer: {
      flex: 1,
    },
  })
