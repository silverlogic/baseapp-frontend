import { Meta } from '@storybook/addon-docs'

<Meta title="@baseapp-frontend-template / Components/Navigation/NavCentered" />

# Component Documentation

## NavCentered

- **Purpose**: A centered navigation component that provides a horizontal menu layout with items aligned in the center of the header.
- **Expected Behavior**: Displays as a horizontal navigation bar with centered menu items on desktop screens, collapsing into a mobile-friendly drawer on smaller screens. Includes logo, navigation items, and optional account menu.

## Use Cases

- **Current Usage**: Used as the main navigation header in applications requiring a centered, horizontal navigation layout.
- **Potential Usage**: Could be used in:
  - Marketing websites
  - Portfolio sites
  - Product landing pages
  - E-commerce platforms
  - Content-focused websites where navigation clarity is priority

## Props

- **navData** (array): Navigation items data containing routes, icons, and labels
- **openNav** (boolean): Whether the navigation is open or closed
- **onCloseNav** (function): Callback function to close the navigation

## Notes

- **Related Components**:
  - Header: Top navigation container
  - AccountMenu: User account management menu
  - VerticalDrawer: Mobile navigation drawer
  - NavigationLayout: Parent layout component

## Example Usage

```javascript
import { NavCentered } from '@baseapp-frontend/design-system'

const MyComponent = () => {
  const [openNav, setOpenNav] = useState(false)
  return (
    <NavCentered
      navData={[
        { title: 'Home', icon: <MenuIcon /> },
        { title: 'About', icon: <InfoIcon /> },
        { title: 'Contact', icon: <ContactIcon /> },
      ]}
      openNav={openNav}
      onCloseNav={() => setOpenNav(false)}
    />
  )
}

export default MyComponent
```
