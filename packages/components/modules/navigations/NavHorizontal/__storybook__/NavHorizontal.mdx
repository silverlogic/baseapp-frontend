import { Meta } from '@storybook/addon-docs'

<Meta title="@baseapp-frontend-template / Components/Navigation/NavHorizontal" />

# Component Documentation

## NavHorizontal

- **Purpose**: A horizontal navigation component that provides a top-level menu layout with items arranged in a horizontal bar.
- **Expected Behavior**: Displays as a horizontal navigation bar with menu items aligned horizontally on desktop screens. Collapses into a mobile-friendly drawer on smaller screens. Supports navigation items with icons, labels, and optional dropdown menus.

## Use Cases

- **Current Usage**: Used as the main navigation header in applications requiring a horizontal navigation layout.
- **Potential Usage**: Could be used in:
  - Corporate websites
  - Web applications
  - Online platforms
  - Dashboard interfaces
  - Sites with simple, flat navigation structures

## Props

- **navData** (NavigationData): Navigation configuration data containing routes, icons, and menu structure
- **openNav** (boolean): Controls the open/closed state of the navigation drawer
- **onCloseNav** (function): Callback function when navigation drawer is closed

## Notes

- **Related Components**:
  - NavVertical: Alternative vertical navigation layout
  - Header: Top navigation container
  - NavigationLayout: Parent layout component
  - VerticalDrawer: Mobile navigation drawer

## Example Usage

```javascript
import { NavHorizontal } from '@baseapp-frontend/design-system'

const MyComponent = () => {
  const [openNav, setOpenNav] = useState(false)
  return (
    <NavHorizontal
      navData={[
        { title: 'Home', icon: <MenuIcon /> },
        { title: 'About', icon: <InfoIcon /> },
        { title: 'Contact', icon: <ContactIcon /> },
      ]}
      openNav={openNav}
      onCloseNav={() => setOpenNav(false)}
    />
  )
}

export default MyComponent
```
