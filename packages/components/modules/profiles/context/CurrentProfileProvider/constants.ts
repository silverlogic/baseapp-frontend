import type { CurrentProfileState } from './types'

export const CURRENT_PROFILE_STORAGE_KEY = 'currentProfile'
export const INITIAL_CURRENT_PROFILE_STATE: CurrentProfileState = {
  profile: undefined,
}
