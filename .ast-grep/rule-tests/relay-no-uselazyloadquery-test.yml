id: relay-no-uselazyloadquery
valid:
  - |
    const data = usePreloadedQuery(ProfileByIdQuery, queryRef)
invalid:
  - |
    const { me } = useLazyLoadQuery<ProfilesListQueryType>(
      ProfilesListQuery,
      { count: 10 },
      { fetchPolicy: 'store-and-network' },
    )
